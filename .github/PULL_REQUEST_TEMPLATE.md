## What

<!-- One or two sentences. What does this PR change? -->

## Why

<!-- The motivation. Bug? Feature? Refactor? Link to the issue if there is one. -->

## How

<!-- Notable design decisions, tradeoffs, or things a reviewer might miss. -->

## Test plan

- [ ] `npm test` passes locally
- [ ] `npm run build` produces no type errors
- [ ] If this touches the parser/IR/simulator: added or updated a fixture
- [ ] If this touches a UI page: manually verified in the browser
- [ ] If this adds a new dialect: included an example config under `examples/`

## Breaking changes

<!-- List anything that changes public API behavior, CLI flags, env vars,
     or trace event schema. -->
